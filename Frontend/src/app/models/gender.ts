export class Gender {
    ci: Number;
    date: Date;
    number: Number
    constructor(ci: number, date: Date, number: Number) {
        this.ci = ci;
        this.date = date;
        this.number = number;
    }
}